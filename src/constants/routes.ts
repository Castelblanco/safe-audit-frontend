export const enum ROUTES {
    INDEX = '/',

    // Auth
    LOGIN = '/auth/login',

    // App
    APP = '/app',
    DASHBOARD = '/app/dashboard',
    AUDITS = '/app/audits',
    AUDIT_CREATE = '/app/audits/create',
    CALENDAR = '/app/calendar',
}
